# Lumen LMS Web

Next.js 15 + React 19 + TypeScript + Tailwind CSS.

## Setup
1. `pnpm install`
2. Create `.env` with `NEXT_PUBLIC_API_URL=http://localhost:4000`
3. `pnpm dev` — runs on http://localhost:3000

Requires the API repo running separately: https://github.com/almas735/lumen-lms-api
